{
  "name": "todo",
  "version": "1.0.0",
  "description": "This is a simple todo list using web storage.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "todo",
    "list"
  ],
  "author": "Tom Kopp",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
